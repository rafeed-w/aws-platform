apiVersion: v1
kind: Service
metadata:
  name: {{ include "thrive-webapp.fullname" . }}
  labels:
    {{- include "thrive-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thrive-webapp.selectorLabels" . | nindent 4 }}